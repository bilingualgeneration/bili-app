import {
    beforeEach,
    describe,
    expect,
    it
} from 'vitest';

import {
    render,
    screen,
    fireEvent,
    waitFor
} from '@testing-library/react';
import {AccountCredentials} from '@/pages/SignUp/AccountCredentials';

describe('SignUp Page', () => {
    describe('Account Credentials Slide', () => {
	it('should render', () => {
	    /*
	    expect(screen.getByItId('account-credentials-slide')).toBeDefined();
	    expect(screen.getByItId('account-credentials-email-input')).toBeDefined();
	    expect(screen.getByItId('account-credentials-password-input')).toBeDefined();
	    */
	});
    });
});
